<!DOCTYPE html>
<html>
<head>
  <title>Draww</title>
  <%= stylesheet_link_tag    "application" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <%= yield :head %>
</head>
<body class="<%= controller_name %> <%= action_name %>">
<header>
	<%= link_to content_tag(:span, "Draww.co"), root_path, :id => 'site_title' %>

	<nav class="user">
		<% if current_user %>
			<%# TODO: Change Links to "My Page | New Image | Logout"
			TODO: Move "Edit Profile" to BIP on users/show %>
			<div class="msg">Hello, <strong><%= current_user.username %></strong></div>
			<%= link_to "Edit Profile", edit_user_path(current_user.id), :class => 'button' %>
			<%= link_to "My Images", name_user_path(current_user.username), :class => 'button green' %>
			<%= link_to "Logout", :logout, :class => 'button light' %>
		<% else %>
			<div class="logged_in">
				<%= render 'user_sessions/form' %>
				<%= link_to "Sign Up", new_user_path, :class => 'button green' %>
			</div>
		<% end %>
	</nav>
</header>
<%= render 'shared/flashes' %>

<div role="main" id="view">
<%= yield %>
</div>

<footer>
	Footer here
	<%# TODO: Add Creative Commons notice + main social media links + contact %>
</footer>
</body>
</html>
